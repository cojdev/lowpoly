const theme = {
    colours: {
        primary: '#e14',
    },
    topbar: {
        height: '40px',
    },
    controls: {
        width: '320px',
    }
}

export default theme;
export const { colours, topbar } = theme;